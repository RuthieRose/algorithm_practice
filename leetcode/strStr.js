var strStr = function(haystack, needle) {
    
 // get needle length ??
 // Can you even search for a substring within a string? 
 // How do you search for "" within something? 
 
 if (needle === "") return 0;
 
 return haystack.indexOf(needle);
 
};