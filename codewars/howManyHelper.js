

function findDigits(n,k) {

 let results = []
 let workingNum = []
 let prevNum = 0

 /* to get sum 
* starting num is 1 or whatever is in the loop, whichever is applicable. 
* Starting digit is subtracted from the sum and then set as the digit
* The next digit is calculated by the prev digit's value being the minimum. The next eligible number is subtracted from the sum. This has to iterate too though. 
* 
 */
 


 return results
}

console.log(findDigits(10,3))